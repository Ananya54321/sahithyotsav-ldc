"use client";

// FloatingParticles removed — not part of Cadbury's clean design
export default function FloatingParticles({ count = 0, className = "" }: { count?: number; className?: string }) {
  return null;
}
